[package]
name = "nurbs-kernel"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
geometry-ops = { path = "../geometry-ops" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

brep-topology = { path = "../brep-topology" }
