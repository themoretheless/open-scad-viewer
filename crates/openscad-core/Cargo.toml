[package]
name = "openscad-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
value-codec = { path = "../value-codec" }
