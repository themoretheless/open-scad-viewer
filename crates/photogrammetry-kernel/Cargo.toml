[package]
name = "photogrammetry-kernel"
version = "0.1.0"
edition = "2021"
publish = false

[features]
gpu = ["dep:gpu-compute"]

[dependencies]
gpu-compute = { path = "../gpu-compute", optional = true }
