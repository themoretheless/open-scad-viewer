[package]
name = "slicer-core"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
gcode-core = { path = "../gcode-core" }
planar-geometry = { path = "../planar-geometry" }

[dev-dependencies]
polygon-core = { path = "../polygon-core" }
