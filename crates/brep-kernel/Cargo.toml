[package]
name = "brep-kernel"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
value-codec = { path = "../value-codec" }
nurbs-core = { path = "../nurbs-core" }
brep-topology = { path = "../brep-topology" }
