Self-authored AP242 `/6` fixtures generated from exact kernel B-reps by
`generate_step_v5_fixture` using the `v6-*` modes. The cylinder, cone and
torus files use standard `SEAM_CURVE` entities with lifted UV trims; sphere
and cone fixtures include pole-safe topology.
