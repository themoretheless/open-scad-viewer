node_modules
dist
.DS_Store
*.js.map
src/**/*.js
*.log
*.tmp
coverage
.env*
.vscode/*.log
.open-scad-viewer.duckdb*
.open-scad-runtime/
/tmp/
tools/manifold-bench/tmp/
tools/manifold-bench/historical/

# Qualification runs are append-only downloadable CI artifacts, never source.
output/qualification/
# Headless rasterizer test snapshots are regenerable artifacts.
output/raster-*.ppm

# Machine-local rbench experiment trees (HTML/JSON reports).
.rbench/

# Machine-specific generated MCP client configurations
.local-integrations/

# Reproducible Rust/WASM NURBS build products
crates/**/target/
src/generated/nurbs-kernel/
src/generated/geometry-kernels/
src/generated/language-kernel/
src/generated/wasm-brotli/
src/generated/harfbuzz/

# Symlinks with these names (worktrees pointing at another checkout) must stay untracked too:
# the directory patterns above match only real directories.
crates/target
src/generated/geometry-kernels
src/generated/language-kernel
src/generated/wasm-brotli
src/generated/harfbuzz
