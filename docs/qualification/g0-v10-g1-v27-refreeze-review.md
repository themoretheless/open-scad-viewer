# G0 v10 / G1 v27 no-claim re-freeze

The append-only B-rep V6 release binds the qualified `nurbs-boolean-bezier-le3/8`
finite cell, including exact-region union and tool-minus-source difference,
the rebuilt own-Rust WASM, product bridge certificates, and explicit tangency
and coincidence refusals.

G0 v1-v9, G1 v1-v26, and every prior status, review, capability plan, evidence,
matrix, index, registry, and release artifact remain byte-immutable. This
re-freeze imports no previous candidate result and makes no semantic-manifold
qualification claim.

The G1 v27 candidate starts with zero completed work units and zero clean runs.
All 4,740 planned units remain mandatory. Tangency and coincidence are not
described as Complete: the current implementation has contact detection but no
complete singular-UV or identical-overlap ownership/removal proof.
