# G0 v8 / G1 v25 Geometry Closure V3 re-freeze

Geometry Closure V3 changes the own-Rust B-rep sources and rebuilt WASM bytes.
G0 v8 binds the current toolchain inputs, while G1 v25 rebinds the existing
finite semantic-manifold contract to the final source and generated bytes.

G0 v1-v7, G1 v1-v24, and every prior status and review remain byte-immutable
historical evidence. This append-only freeze performs no external clean run:
zero of 4740 work units and zero clean runs are complete.

Prior results remain discovery-only and cannot be imported.
`qualificationClaim` remains `none`, qualification approval remains
`not-approved`, u07 remains unresolved, and production cutover remains
prohibited.
