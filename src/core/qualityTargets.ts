/** Deterministic scale targets exercised in CI without wall-clock assertions. */
export const QUALITY_TARGETS = Object.freeze({
  sceneEntities: 1_000,
  renderTriangles: 750_000,
  generatedCompilerCases: 256,
})
