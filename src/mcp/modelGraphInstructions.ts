/** Shared onboarding for local and HTTP clients; do not imply a browser connection. */
export const MODELGRAPH_INSTRUCTIONS = `For new parametric models, prefer ModelGraph/1. First call modelgraph_language: it returns the language guide, JSON Schema, examples, workflow and limitations. Do not invent operations or parameters. For compact human-readable source, read text_guide and call modelgraph_text_compile; use its returned document with check/report/export. Compact ranges accept .. or ..< followed by by step OR count amount; generators preserve separate parts at the root and are bounded. Read text_guide for for/where/let, zip/enumerate and collection limits.
Use modelgraph_generate for gears, planetary sets and threads; read openscad://language/modelgraph-mechanical for the generator contract. Use modelgraph_nurbs_language before rational curves or surfaces.
Workflow: obtain dimensions and units, create a document with editable parameters, modelgraph_compile, modelgraph_check, then modelgraph_report. Inspect checks (passed/failed/unknown), measurements, constraints, mechanical reports and PNG views; correct the document and repeat when necessary. Use modelgraph_set_parameters with the returned document hash for parameter changes. modelgraph_modify supports its advertised operations only.
Use modelgraph_interference for current-position assembly volume intersections (at most 32 leaf components); unknown is not a successful clearance check. Contact, minimum clearance, swept motion and physical printability are not certified.
Fluent validate/assert chains are documented in text_guide. Scalar constraints run before building; geometry_assertions run on the actual target meshes in check/report and before export. Compilation alone does not prove geometry assertions. Failed or unknown checks must be resolved before export.
Export with modelgraph_export. 3MF preserves separate scene meshes at their world positions using lossless compression; a Boolean union is not automatically separated into original components. Limits: 100000 triangles and 4 MiB per artifact. NURBS uses modelgraph_nurbs_export; STEP is unavailable. Convert an existing STL/OBJ/PLY/OFF/AMF/3MF file between formats with mesh_convert.
MCP modeling is headless. Browser drafts, camera and scanning-plane controls are not synchronized or controlled by these tools. Never claim to have changed the visible browser model. Language resources must be read explicitly; their full contents are not automatically injected into a client conversation.`
